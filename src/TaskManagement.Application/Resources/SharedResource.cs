namespace TaskManagement.Application.Resources;

// Empty marker class — IStringLocalizer<SharedResource> binds to the co-located
// SharedResource.resx / SharedResource.mt.resx files via the default resource
// manager naming convention. Resource keys live in those .resx files (validator
// messages, domain error-code descriptions, Identity errors).
public sealed class SharedResource
{
}
