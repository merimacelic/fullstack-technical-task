# Build-time fallback used when no runtime config is injected.
# The dev server reads .env.development instead; production containers write
# /env.js at startup from real env vars (docker-entrypoint.sh). See README.
VITE_API_BASE_URL=http://localhost:8080
